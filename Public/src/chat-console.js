(()=>{const params=new URLSearchParams(location.search);const guide=params.has('guide');document.body.classList.add('xbox-chat');if(guide)document.body.classList.add('guide-chat');let attempts=0;const timer=setInterval(()=>{if(typeof window.switchView!=='function'||typeof window.switchToDM!=='function'){if(++attempts>100)clearInterval(timer);return}clearInterval(timer);if(guide){window.switchView('dms');const conversations=document.createElement('section');conversations.className='guide-conversations';document.querySelector('.sidebar').prepend(conversations);function renderDMs(){conversations.replaceChildren();const heading=document.createElement('h3');heading.textContent='Recent conversations';conversations.append(heading);for(const dm of window.novaGuideConversations||[]){if(!dm.otherUser)continue;const b=document.createElement('button'),name=document.createElement('b'),preview=document.createElement('small');name.textContent=dm.otherUser;preview.textContent=String(dm.lastMessage||'Open conversation').slice(0,85);b.append(name,preview);b.onclick=()=>window.switchToDM(dm.otherUser);conversations.append(b)}const expand=document.createElement('button');expand.textContent='Open full Chat ↗';expand.onclick=()=>parent.postMessage({novaAction:'expandGuideChat'},location.origin);conversations.append(expand)}renderDMs();addEventListener('nova-dm-list',renderDMs);const back=document.createElement('button');back.className='guide-chat-back';back.textContent='‹ Messages';back.onclick=()=>document.body.classList.remove('guide-conversation');document.body.append(back);const original=window.switchToDM;window.switchToDM=(...args)=>{original(...args);document.body.classList.add('guide-conversation')};}const report=params.get('report');if(report){document.body.classList.add('report-only');window.switchChannel('report');const desc=document.getElementById('reportDescription'),type=document.getElementById('reportType');if(desc)desc.value='Item: '+report+'\n'+(params.get('url')||'')+'\n\nIssue: ';if(type)type.value=params.get('type')||'Game Issue';}},200)})();
